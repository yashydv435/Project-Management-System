# Root railway.toml — not used for deployment.
# Railway services are configured individually:
#   - backend/railway.toml  → Backend service (Express + Prisma)
#   - frontend/railway.toml → Frontend service (Next.js)
# See the Railway deployment guide in the README.
